<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dabbssolutions.farmwalayuser">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Farmwalay">
        <activity android:name=".activities.ActivitySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivityForgotPass" >

        </activity>
        <activity android:name=".activities.ActivityLogin" >

        </activity>
        <activity android:name=".activities.ActivitySignup" >

        </activity>

        <activity android:name=".activities.MainActivity" >

        </activity>
        <activity android:name=".activities.FarmHouseDetailsActivity" >

        </activity>
        <activity android:name=".activities.ActivityAddUsers" >

        </activity>
    </application>

</manifest>